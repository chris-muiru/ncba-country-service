apiVersion: v1
kind: Service
metadata:
  name: ncba-service
  namespace: default
  labels:
    app: ncba-app
spec:
  type: ClusterIP
  selector:
    app: ncba-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
